# UI设计规范 (Basic Design Standards)

## 1. 基础规范 (Basic Standards)

### 1⃣ 栅格系统 (Grid System)
- **移动端**：采用 **12列** 栅格布局。
- **PC端**：采用 **24列** 栅格布局。
- **响应式**：直接套用栅格系统，确保多端适配不冲突。

### 2⃣ 间距法则 (Spacing Rules)
- **核心原则**：所有间距必须是 **8的倍数** (8/16/24/32px)。
- **转换单位 (1px = 2rpx)**：
  - **8px** -> `16rpx` (极小间距)
  - **16px** -> `32rpx` (小间距)
  - **24px** -> `48rpx` (中等间距)
  - **32px** -> `64rpx` (大间距)
- **魔法数字**：坚持使用 `8px (16rpx)` 的倍数，提升页面整洁度与韵律感。

### 3⃣ 色彩系统 (Color System)
- **主色 (Primary)**：占 **60%-70%** (品牌色，稳固视觉基调)。
- **辅色 (Secondary)**：占 **15%-20%** (点缀、按钮、强调操作)。
- **中性色 (Neutral)**：占 **10%-20%** (文字、边框、背景)。
- **无障碍标准**：文字和背景对比度 **≥ 4.5:1**。

#### A. 主色调 (Brand Colors)
| 色彩名称 | 色值 | 用途 |
| :--- | :--- | :--- |
| **电光紫** | `#8B6DFF` | 核心主色，用于主按钮、选中态、关键图标 |
| **深邃紫** | `#7052CC` | 点击态、深色背景元素 |
| **主渐变** | `linear-gradient(90deg, #B4C0FF 0%, #8E9EFF 100%)` | 主CTA按钮、页面头部背景、重要卡片背景 |

#### B. 辅助色 (Accent Colors)
| 色彩名称 | 色值 | 用途 |
| :--- | :--- | :--- |
| **甜心粉** | `#FF85C0` | 女性化标签、点赞、热门活动 |
| **天空青** | `#5AD8F2` | 信息展示、新手引导、辅助标签 |
| **活力黄** | `#FFD43B` | 金额、VIP会员、积分、强提醒 |
| **功能红** | `#FF4D4F` | 错误、拒绝、删除 |
| **功能绿** | `#2CC995` | 成功、通过、安全 |

#### C. 中性色 (Neutrals)
| 色彩名称 | 色值 | 用途 |
| :--- | :--- | :--- |
| **主要文字** | `#333333` | 标题、强调信息 |
| **常规文字** | `#666666` | 正文、列表内容 |
| **次要文字** | `#999999` | 辅助说明、日期、占位符 |
| **背景色** | `#F5F6F8` | 全局页面背景（极浅的灰紫调白） |
| **卡片背景** | `#FFFFFF` | 内容承载底色 |
| **分割线** | `#F0F0F0` | 模块分割、边框 |

### 4⃣ 字体规范 (Typography)
- **字体家族**：优先使用系统默认字体（iOS: 苹方 / Android: 思源黑体）。
- **层级锁死** (1px = 2rpx)：
  - **标题**：`24px` -> `48rpx` (字重 600/Bold)
  - **副标题**：`20px` -> `40rpx` (字重 500/Medium)
  - **正文**：`16px` -> `32rpx` (字重 400/Regular)
  - **辅助文字**：`14px` -> `28rpx` (字重 400/Regular)
- **行高**：字号 × 1.5 ~ 2倍。

---

## 2. 核心组件库 (Core Components) - 覆盖80%高频场景

### 1⃣ 按钮 (Buttons)
- **三种尺寸**：
  - **小 (Small)**：`32px` -> `64rpx`
  - **中 (Medium)**：`40px` -> `80rpx`
  - **大 (Large)**：`48px` -> `96rpx`
- **四种状态**：默认 (Default) / 悬停 (Hover) / 禁用 (Disabled) / 加载 (Loading)。
- **样式细节**：
  - **圆角**：统一 `8px` -> `16rpx`。
  - **禁用态**：透明度调至 **50%**。

### 2⃣ 输入框 (Inputs)
- **高度**：统一 `48px` -> `96rpx`。
- **状态**：
  - **聚焦**：边框变 `2px` (`4rpx`) 主色。
  - **错误**：红色边框 + 警示图标。
- **功能**：字数限制实时显示在右侧。

### 3⃣ 弹窗 & Toast (Modals)
- **遮罩层**：透明度 **60%** (`opacity: 0.6`)。
- **圆角**：`12px` -> `24rpx`。
- **Toast**：
  - 自动消失时间：**2-3秒**。
  - 位置：统一（顶部或居中）。

### 4⃣ 图标 (Iconography)
- **技术选型**：UnoCSS Preset Icons
- **图标库**：
  - [Tabler Icons](https://tabler.io/icons) (前缀: `i-tabler`) - 默认推荐，风格中性现代
  - [Remix Icon](https://remixicon.com/) (前缀: `i-ri`) - 备选，互联网风格强
- **使用方式**：
  ```html
  <!-- 基础用法 -->
  <view class="i-tabler-home text-xl text-primary"></view>
  
  <!-- 自定义大小颜色 -->
  <view class="i-tabler-user text-32rpx text-[#333]"></view>
  ```
- **常用尺寸**：
  - `16px` -> `32rpx` (辅助图标)
  - `24px` -> `48rpx` (常规图标)
  - `32px` -> `64rpx` (大图标/入口)

---

## 3. CSS 变量参考 (CSS Variables)

```css
page {
    /* 间距系统 (Spacing) */
    --space-base: 16rpx;  /* 8px */
    --space-md: 32rpx;    /* 16px */
    --space-lg: 48rpx;    /* 24px */
    --space-xl: 64rpx;    /* 32px */

    /* 字体大小 (Typography) */
    --text-title: 48rpx;  /* 24px */
    --text-subtitle: 40rpx; /* 20px */
    --text-body: 32rpx;   /* 16px */
    --text-aux: 28rpx;    /* 14px */

    /* 圆角 (Radius) */
    --radius-base: 16rpx; /* 8px */
    --radius-modal: 24rpx; /* 12px */

    /* 组件尺寸 */
    --btn-h-s: 64rpx;
    --btn-h-m: 80rpx;
    --btn-h-l: 96rpx;
    --input-h: 96rpx;
}
```
